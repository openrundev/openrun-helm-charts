{{- $adminCreds := include "openrun.adminCredentials" . | fromYaml }}
Thank you for installing OpenRun.

1. API endpoint:
   kubectl get svc {{ include "openrun.fullname" . }} -n {{ .Release.Namespace }}
   In-cluster URLs:
   - HTTP:  http://{{ include "openrun.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.config.http.port }}
   - HTTPS: https://{{ include "openrun.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.config.https.port }}

{{- if $adminCreds.autoGenerated }}
2. Admin credentials:
   - Username: {{ .Values.config.adminUser }}
   - Generated password: {{ $adminCreds.password }}
   Record this password securely; it is only shown once after installation.
{{- end }}

Review charts/openrun/README.md for configuration examples. Docs are at https://openrun.dev/docs/container/kubernetes/.
